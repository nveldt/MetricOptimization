function [A,truth] = readinLFR(netfile,comfile)
% [A,truth] = readinLFR(filename): reads in an LFR network and outputs the
% adjacency matrix corresponding to that network and the ground truth
% community vector

net = fopen(netfile,'r');



end